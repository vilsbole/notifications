name: CI

on: [push]

jobs:
  server:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build the Docker image
        working-directory: ./server
        run: docker build . --file Dockerfile --tag search-service-test:$(date +%s) --target test
